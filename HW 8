//Hannah Neymeyer
//Dr. Litman
//CSC 250

public class sortOnnameBubble
{
     public static void main(String[ ] args)
     {
             String[ ] names = BibleBookCollection;
             sortStringBubble (BookName);
             for ( int k = 0;  k < 4;  k++ )
                System.out.println( BookName [ k ] );
      }

      public static void sortOnname( String  x [ ] )
      {
            int j;
            boolean flag = true; 
            String temp;

            while ( flag )
            {
                  flag = false;
                  for ( j = 0;  j < x.length - 1;  j++ )
                  {
                          if ( x [ j ].compareToIgnoreCase( x [ j+1 ] ) > 0 )
                          {                                            
                                      temp = x [ j ];
                                      x [ j ] = x [ j+1];     
                                      x [ j+1] = temp;
                                      flag = true;
                                      
                                      int[] sortOnChapterCount = BibleBookChapter;
                                      int temp;

                                       for (int i = 0; i < numbers.length; i++)
                                      {
                                       for(int j = 0; j < numbers.length; j++)
                                       {
                                          if(numbers[i] > numbers[j + 1])
                                            {
                                                       temp = numbers [j + 1];
                                                         numbers [j + 1]= numbers [i];
                                                             numbers [i] = temp;
                                                                                              }
                                                                                              }
                                                                                              }

                                                       for (int i = 0; i < numbers.length; i++)
                                                                                             {
                                                      System.out.println(numbers[i].toString());
                                                      
                                                      
                                                      public void display(sortOnnameBubble) {
	                                                    System.out.print(sortOnnameBubble + sortOnChapterCount);  
                           }
                   }
            }
      }
}
